<h3>フォロワー</h3>
<ul>
  <% if @followers.count != 0 %>
    <% @followers.each do |follower| %>
      <li><%= link_to follower.name, user_path(follower.id) %></li>
    <% end %>
  <% else %>
    <p>まだフォロワーがいません</p>
  <% end %>
</ul>